# Validated runtime: CPython 3.10 in the pandaai environment.
# panda_data is a private package and must be available through the configured
# package index or pre-installed before running the live portfolio scan.
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.7.2
panda_data==0.0.12
pytest==9.0.2
